spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
  security:
    user:
      name: testuser
      password: testpassword

app:
  jwt:
    secret: your-very-secret-key-that-is-at-least-32-characters-long
  gateway:
    secret: testsecret
  user:
    username: normaluser
    email: myuser@example.com
    password: password
  admin:
    username: adminuser
    email: admin@example.com
    password: password
