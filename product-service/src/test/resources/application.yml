spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/product_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
  security:
    user:
      name: testuser
      password: testpassword

app:
  gateway:
    secret: testsecret